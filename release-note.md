# Release Notes

## نسخه 5.0.0 - 1405/06/08 (2026-08-30)

### قابلیت جدید: تایم پیکر مستقل خارج از پاپ‌آور (`MdsPersianTimePicker`)

تا پیش از این، تنها راه انتخاب ساعت، تکس‌باکس داخل خودِ پاپ‌آور دیت‌پیکر بود. حالا یک کلاس کاملاً مستقل به نام
`MdsPersianTimePicker` اضافه شده که می‌توان آن را روی **هر تکس‌باکس دلخواهی** (خارج از پاپ‌آور) سوار کرد و در صورت
نیاز به یک `MdsPersianDateTimePicker` متصل کرد.

```js
// روی خود دیت پیکر: enableTimePicker را فعال کنید ولی تکس‌باکس داخلی پاپ‌آور را مخفی کنید
const datePicker = new MdsPersianDateTimePicker(document.querySelector('#myDate'), {
  enableTimePicker: true,
  showTimePickerInPopover: false,
  groupId: 'order-date-time',
});

// یک تکس‌باکس جدا برای ساعت، با همان groupId
const timePicker = new MdsPersianTimePicker(document.querySelector('#myTime'), {
  groupId: 'order-date-time',
  onChange: (hhmm) => console.log('ساعت جدید:', hhmm), // مثلا 1430
});
```

- تایپ در تکس‌باکس ساعت به‌صورت خودکار فرمت `HH:mm` می‌گیرد (کلیدهای بالا/پایین ساعت یا دقیقه را step می‌کنند،
  فوکوس کل متن را انتخاب می‌کند، ورودی نامعتبر روی خروج فوکوس پاک می‌شود).
- با اتصال از طریق `groupId`، هر تغییر معتبر ساعت در تکس‌باکس، در `selectedDate`/`selectedDateToShow` دیت‌پیکر
  متصل هم اعمال می‌شود؛ و برعکس، اگر ساعتِ دیت‌پیکر از جای دیگری (مثلا با متد `setTime`) تغییر کند، تکس‌باکس
  خارجی هم به‌روز می‌شود.
- **نکته مهم:** برای اینکه ساعت هنگام انتخاب روز از تقویم پاک نشود، باید `enableTimePicker: true` روی
  دیت‌پیکر ست شود؛ `showTimePickerInPopover: false` فقط نمایش تکس‌باکس داخلی پاپ‌آور را مخفی می‌کند، نه ردیابی ساعت را.

### دمو

یک بخش جدید با عنوان «External Time Picker (MdsPersianTimePicker)» به `demo/demo.html`/`demo/demo.js` اضافه شده
که همین قابلیت را به‌صورت زنده نشان می‌دهد: یک آیکون تقویم (بدون تکس‌باکس ساعت داخلی) به‌همراه یک تکس‌باکس ساعت
مستقل که با `groupId` یکسان به آن متصل است.

### آپشن جدید روی `MdsPersianDateTimePickerSetting`

- `showTimePickerInPopover` (پیش‌فرض `true`): وقتی `false` باشد، تکس‌باکس ساعتِ داخل پاپ‌آور نمایش داده نمی‌شود
  (برای استفاده از `MdsPersianTimePicker` مستقل).

### متد عمومی جدید روی `MdsPersianDateTimePicker`

- `setTime(hour, minute)`: تنظیم دستی ساعت/دقیقه‌ی تاریخ انتخاب شده؛ برای اتصال به یک `MdsPersianTimePicker`
  خارجی استفاده می‌شود ولی به‌صورت مستقیم هم قابل فراخوانی است.

### بازنویسی تایم پیکر داخل پاپ‌آور

اینپوت `type="time"` مرورگر (که ظاهر و رفتارش در مرورگرهای مختلف متفاوت و برای کاربر گیج‌کننده بود) با یک تکس‌باکس
ماسک‌دار جایگزین شد؛ رفتار آن دقیقاً مشابه کامپوننت `time-picker` پروژه‌ی Karnama.Angular است:

- تایپ عدد به‌صورت خودکار فرمت `HH:mm` می‌گیرد و بین ساعت/دقیقه به‌صورت خودکار `:` اضافه می‌شود.
- کلیدهای بالا/پایین صفحه‌کلید، بسته به موقعیت مکان‌نما، ساعت یا دقیقه را step می‌کنند.
- فوکوس روی تکس‌باکس، کل متن را انتخاب می‌کند؛ خروج فوکوس با مقدار نامعتبر، تکس‌باکس را پاک می‌کند.
- پشتیبانی از اعداد فارسی هنگام تایپ.

### رفع باگ: از دست رفتن ساعت هنگام بسته و باز شدن دوباره‌ی پاپ‌آور

اگر ساعت تغییر می‌کرد و بدون انتخاب روز، پاپ‌آور بسته و دوباره باز می‌شد، ساعت به مقدار قبلی برمی‌گشت. علتش
این بود که Bootstrap Popover محتوای خودش را فقط یک‌بار می‌سازد و تغییر ساعت باعث بازسازی آن محتوا نمی‌شد
(بر خلاف انتخاب روز که این بازسازی برایش انجام می‌شد). حالا با بسته شدن پاپ‌آور، محتوای ذخیره‌شده‌ی آن هم با
آخرین مقدار ساعت هماهنگ می‌شود.

### رفع باگ: افت کاراکتر هنگام تایپ سریع

پیاده‌سازی اولیه‌ی ماسک ساعت، اضافه شدن `:` را با یک `setTimeout` کوتاه انجام می‌داد؛ در تایپ خیلی سریع (یا
شبیه‌سازی تایپ در تست خودکار) ممکن بود یک رقم یا `:` از دست برود. الان کل منطق ماسک به‌صورت کاملاً همزمان
(sync) پیاده‌سازی شده و این وابستگی به زمان‌بندی حذف شده است.
